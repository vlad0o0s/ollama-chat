# Аудит функциональности создания изображений через ИИ (img-to-img)

## Дата аудита: 2026-01-13

## Обзор архитектуры

Текущая реализация использует:
- **LLaVA** (через Ollama) для анализа исходного изображения
- **GPT-OSS** (через Ollama) для генерации промптов и определения настроек KSampler
- **ComfyUI** с моделью **Flux1-dev-fp8** для генерации изображений
- **Workflow шаблон** `Flux-img-to-img.json` для img-to-img генерации

---

## КРИТИЧЕСКИЕ ОШИБКИ

### 1. ❌ Неправильная обработка размеров изображения в img-to-img

**Проблема:**
- В img-to-img режиме система пытается установить размеры изображения из запроса пользователя
- Но для img-to-img **должны использоваться размеры исходного изображения**, а не запрошенные
- Изменение размеров может привести к искажению композиции и потере качества

**Местоположение:** `backend/app/services/comfyui_service.py:512-525`

**Текущий код:**
```python
# Обновляем размеры изображения (если есть соответствующие ноды)
size_updated = False
for node_id, node_data in workflow.items():
    if isinstance(node_data, dict) and "inputs" in node_data:
        inputs = node_data.get("inputs", {})
        if "width" in inputs and "height" in inputs:
            workflow[node_id]["inputs"]["width"] = width  # ❌ НЕПРАВИЛЬНО для img-to-img
            workflow[node_id]["inputs"]["height"] = height
```

**Правильное решение:**
- Для img-to-img нужно **определить размеры исходного изображения** и использовать их
- Или вообще не устанавливать размеры, позволив ComfyUI использовать размеры исходного изображения
- Размеры должны устанавливаться только для text-to-img

**Рекомендация:** Добавить определение размеров исходного изображения через PIL/Pillow и использовать их для img-to-img workflow.

---

### 2. ❌ Отсутствие валидации формата изображения перед загрузкой в ComfyUI

**Проблема:**
- Система не проверяет формат и размеры изображения перед загрузкой в ComfyUI
- Могут быть проблемы с очень большими изображениями или неподдерживаемыми форматами

**Местоположение:** `backend/app/services/comfyui_service.py:661-700`

**Рекомендация:** Добавить валидацию:
- Максимальный размер (например, 4096x4096)
- Поддерживаемые форматы (PNG, JPEG, WEBP)
- Проверка на поврежденные файлы

---

### 3. ⚠️ Seed всегда случайный (0) в img-to-img

**Проблема:**
- Seed устанавливается в 0 (случайный) для каждой генерации
- Нет возможности воспроизвести результат
- Для отладки и сравнения результатов нужна возможность задавать seed

**Местоположение:** `backend/app/services/comfyui_service.py:563-564`

**Текущий код:**
```python
if "seed" in node_data["inputs"]:
    node_data["inputs"]["seed"] = 0  # Случайный seed
```

**Рекомендация:** 
- Добавить параметр `seed` в `ksampler_settings`
- Если seed не указан, использовать случайный
- Сохранять seed в метаданных для возможности воспроизведения

---

## СЕРЬЕЗНЫЕ ПРОБЛЕМЫ

### 4. ⚠️ Неоптимальные значения denoise по умолчанию

**Проблема:**
- В fallback случаях используется `denoise: 0.5`, что слишком низко для большинства трансформаций
- Для изменения цвета нужен минимум 0.8-0.9, но fallback может вернуть 0.5

**Местоположение:** `backend/app/services/prompt_service.py:829-850`

**Текущий код:**
```python
# Fallback: используем значения по умолчанию
return {
    "denoise": 0.5,  # ❌ Слишком низко
    "steps": 30,
    "cfg": 1.0,
    "sampler_name": "euler",
    ...
}
```

**Рекомендация:** 
- Увеличить fallback denoise до 0.65-0.7 для лучших результатов по умолчанию
- Или использовать более умный fallback на основе анализа запроса

---

### 5. ⚠️ Отсутствие обработки случая, когда размеры исходного изображения не совпадают с запрошенными

**Проблема:**
- Если пользователь запрашивает размеры, отличные от исходного изображения, система все равно пытается их установить
- Это может привести к искажению или потере качества

**Рекомендация:**
- Для img-to-img игнорировать запрошенные размеры
- Использовать размеры исходного изображения
- Или добавить предупреждение пользователю, что размеры будут изменены

---

### 6. ⚠️ Недостаточная детализация промпта для LLaVA

**Проблема:**
- Хотя системный промпт улучшен, пользовательский промпт может быть более структурированным
- LLaVA может пропустить важные детали, если промпт не достаточно четкий

**Местоположение:** `backend/app/services/prompt_service.py:423-434`

**Рекомендация:**
- Добавить примеры в промпт для LLaVA
- Использовать структурированный формат (списки, категории)
- Добавить проверку полноты описания (есть ли цвета, материалы, объекты)

---

## УЛУЧШЕНИЯ И РЕКОМЕНДАЦИИ

### 7. 💡 Добавить поддержку ControlNet для более точного контроля

**Текущее состояние:** Не используется ControlNet

**Рекомендация:**
- Интегрировать ControlNet для более точного контроля над генерацией
- Особенно полезно для сохранения композиции при изменении цветов/материалов
- Может значительно улучшить качество результатов

**Источник:** [ComfyUI ControlNet Tutorial](https://comfyui-wiki.com/ru/tutorial/advanced/how-to-install-and-use-controlnet-models-in-comfyui)

---

### 8. 💡 Добавить поддержку LoRA для специфических стилей/объектов

**Текущее состояние:** Не используется LoRA

**Рекомендация:**
- Добавить возможность использования LoRA для специфических материалов или стилей
- Например, LoRA для дерева, металла, конкретных цветов
- Может улучшить качество трансформации материалов

---

### 9. 💡 Улучшить обработку ошибок при анализе изображения

**Текущее состояние:** Если LLaVA не может проанализировать, генерация останавливается

**Рекомендация:**
- Добавить fallback: если LLaVA недоступна, использовать базовое описание на основе метаданных изображения
- Или позволить пользователю ввести описание вручную
- Добавить retry механизм с экспоненциальной задержкой

---

### 11. 💡 Добавить валидацию промптов перед отправкой в ComfyUI

**Текущее состояние:** Промпты отправляются без валидации

**Рекомендация:**
- Проверять длину промпта (максимум для Flux)
- Проверять на наличие запрещенных символов
- Валидировать формат промпта

---

### 12. 💡 Улучшить логирование для отладки

**Текущее состояние:** Логирование есть, но можно улучшить

**Рекомендация:**
- Добавить логирование полных промптов (не только первых 100 символов)
- Логировать все настройки KSampler перед отправкой
- Добавить метрики времени выполнения каждого этапа

---

### 13. 💡 Добавить поддержку batch processing

**Текущее состояние:** Генерируется одно изображение за раз

**Рекомендация:**
- Добавить возможность генерации нескольких вариантов с разными seed
- Позволит пользователю выбрать лучший результат
- Особенно полезно для экспериментов с настройками
- добавиь кнопку создания нескольких вариантов

---

### 14. 💡 Оптимизировать использование GPU ресурсов

**Текущее состояние:** Много переключений процессов

**Рекомендация:**
- Группировать запросы к Ollama (LLaVA + GPT-OSS) для минимизации переключений
- Использовать пул соединений для ComfyUI
- Кэшировать результаты анализа изображений

---

## СРАВНЕНИЕ С ЛУЧШИМИ ПРАКТИКАМИ

### ✅ Что сделано правильно:

1. **Использование визуального анализа (LLaVA)** - правильный подход для понимания исходного изображения
2. **Динамическое определение denoise** - хорошая идея использовать LLM для определения оптимальных настроек
3. **Обязательный анализ изображения** - правильно, что генерация не продолжается без анализа
4. **Английский язык для промптов** - правильно для работы с ComfyUI
5. **Высокие значения denoise для изменения цвета** - соответствует лучшим практикам (0.8-0.9)

### ❌ Что отличается от лучших практик:

1. **Размеры изображения** - должны сохраняться из исходного, а не устанавливаться новые
2. **Seed** - должен быть настраиваемым для воспроизводимости
3. **ControlNet** - не используется, хотя может значительно улучшить результаты
4. **Валидация изображений** - отсутствует проверка формата и размеров

---

## ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

### Критический приоритет (исправить немедленно):
1. ✅ **Размеры изображения в img-to-img** - использовать размеры исходного изображения
2. ✅ **Валидация формата изображения** - добавить проверки перед загрузкой

### Высокий приоритет (исправить в ближайшее время):
3. ⚠️ **Seed управление** - добавить возможность задавать seed
4. ⚠️ **Fallback denoise** - увеличить до 0.65-0.7

### Средний приоритет (улучшения):
6. 💡 **ControlNet интеграция** - для лучшего контроля
7. 💡 **LoRA поддержка** - для специфических стилей
8. 💡 **Batch processing** - для генерации нескольких вариантов
9. 💡 **Улучшенное логирование** - для отладки

---

## ЗАКЛЮЧЕНИЕ

Текущая реализация имеет **хорошую архитектуру** с использованием LLaVA для анализа и GPT-OSS для генерации промптов. Однако есть **критические ошибки** в обработке размеров изображений и несколько **серьезных проблем** с настройками по умолчанию.

**Основные проблемы:**
- Неправильная обработка размеров в img-to-img
- Отсутствие валидации изображений
- Неоптимальные fallback значения

**Основные улучшения:**
- Добавить ControlNet для лучшего контроля
- Кэширование описаний изображений
- Улучшенное управление seed

После исправления критических ошибок система должна работать значительно лучше и соответствовать лучшим практикам для img-to-img генерации.

